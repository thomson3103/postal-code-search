# CLAUDE.md — 郵便番号検索サイト（79サイト群 #11）

## プロジェクト概要

日本全国の郵便番号を検索できる静的Webサイト。
日本郵便の公式CSVデータ（著作権フリー）をPythonで解析し、全郵便番号を個別HTMLページとして静的生成する。

**目的**: ロングテールSEOで「〒XXX-XXXX」「○○市 郵便番号」等の検索流入を獲得
**ポジション**: 79サイト群の第2陣サイト（GitHubデータ系）

---

## データソース

### メインデータ: 日本郵便 公式CSV（UTF-8版）

- **URL**: https://www.post.japanpost.jp/zipcode/dl/utf-zip.html
- **ファイル**: utf_ken_all.zip → utf_ken_all.csv
- **レコード数**: 約124,000行
- **更新頻度**: 毎月末
- **ライセンス**: 「郵便番号データに限っては日本郵便株式会社は著作権を主張しません。自由に配布していただいて結構です。」

### CSVカラム構成（UTF-8版・1レコード1行）

```
0: 全国地方公共団体コード（JIS X0401, X0402）
1: （旧）郵便番号（5桁）
2: 郵便番号（7桁）
3: 都道府県名カナ（全角カタカナ）
4: 市区町村名カナ（全角カタカナ）
5: 町域名カナ（全角カタカナ）
6: 都道府県名（漢字）
7: 市区町村名（漢字）
8: 町域名（漢字）
9: 一町域が二以上の郵便番号（0/1）
10: 小字毎に番地が起番（0/1）
11: 丁目を有する町域（0/1）
12: 一つの郵便番号で二以上の町域（0/1）
13: 更新の表示（0:変更なし, 1:変更あり, 2:廃止）
14: 変更理由（0〜6）
```

### 事業所データ（オプション・Phase 2）

- **URL**: https://www.post.japanpost.jp/zipcode/dl/jigyosyo/index.html
- 大口事業所の個別郵便番号（約2万件）
- Phase 1では対応しない。Phase 2で追加。

---

## サイト構造

### URL設計

```
/                           → トップ（都道府県一覧 + 検索窓）
/tokyo/                     → 都道府県ページ（市区町村一覧）
/tokyo/chiyoda-ku/          → 市区町村ページ（町域＋郵便番号一覧）
/zipcode/1000001/           → 個別郵便番号ページ
/search/                    → 検索ページ（JS駆動）
/sitemap.xml                → サイトマップ
```

### ディレクトリ構成（都道府県のスラッグ）

```python
PREFECTURE_SLUGS = {
    "北海道": "hokkaido",
    "青森県": "aomori",
    "岩手県": "iwate",
    "宮城県": "miyagi",
    "秋田県": "akita",
    "山形県": "yamagata",
    "福島県": "fukushima",
    "茨城県": "ibaraki",
    "栃木県": "tochigi",
    "群馬県": "gunma",
    "埼玉県": "saitama",
    "千葉県": "chiba",
    "東京都": "tokyo",
    "神奈川県": "kanagawa",
    "新潟県": "niigata",
    "富山県": "toyama",
    "石川県": "ishikawa",
    "福井県": "fukui",
    "山梨県": "yamanashi",
    "長野県": "nagano",
    "岐阜県": "gifu",
    "静岡県": "shizuoka",
    "愛知県": "aichi",
    "三重県": "mie",
    "滋賀県": "shiga",
    "京都府": "kyoto",
    "大阪府": "osaka",
    "兵庫県": "hyogo",
    "奈良県": "nara",
    "和歌山県": "wakayama",
    "鳥取県": "tottori",
    "島根県": "shimane",
    "岡山県": "okayama",
    "広島県": "hiroshima",
    "山口県": "yamaguchi",
    "徳島県": "tokushima",
    "香川県": "kagawa",
    "愛媛県": "ehime",
    "高知県": "kochi",
    "福岡県": "fukuoka",
    "佐賀県": "saga",
    "長崎県": "nagasaki",
    "熊本県": "kumamoto",
    "大分県": "oita",
    "宮崎県": "miyazaki",
    "鹿児島県": "kagoshima",
    "沖縄県": "okinawa",
}
```

### 市区町村のスラッグ生成ルール

市区町村名のカナ読みをローマ字変換してスラッグにする。
同一都道府県内で重複する場合はJISコード下3桁を付加。

```python
# 例: "千代田区" → "chiyoda-ku"
# 例: "港区" → "minato-ku"
# 例: "札幌市中央区" → "sapporo-shi-chuo-ku"
```

カナ→ローマ字変換は `pykakasi` を使用する。

---

## ページ設計

### 1. トップページ（`/index.html`）

```
[h1] 郵便番号検索 - 全国の郵便番号をかんたん検索
[検索窓] 郵便番号 or 住所で検索（JSインクリメンタルサーチ）
[地方別ブロック]
  北海道・東北: 北海道 | 青森 | 岩手 | ...
  関東: 茨城 | 栃木 | 群馬 | 埼玉 | 千葉 | 東京 | 神奈川
  ...
[総収録件数] 全国 XX,XXX件の郵便番号を収録
```

### 2. 都道府県ページ（`/{prefecture}/index.html`）

```
[h1] 東京都の郵便番号一覧
[パンくず] トップ > 東京都
[市区町村一覧テーブル]
  市区町村名 | 郵便番号件数 | 代表郵便番号
  千代田区    | 42件        | 100-0001〜100-0014
  中央区      | 38件        | 103-0001〜104-0061
  ...
[都道府県の基本情報]
  県庁所在地、面積、人口（静的に埋め込み）
```

### 3. 市区町村ページ（`/{prefecture}/{city}/index.html`）

```
[h1] 東京都千代田区の郵便番号一覧
[パンくず] トップ > 東京都 > 千代田区
[郵便番号テーブル（五十音順）]
  郵便番号    | 町域名      | 読み
  〒100-0001 | 千代田      | チヨダ
  〒100-0002 | 皇居外苑    | コウキョガイエン
  〒100-0003 | 一ツ橋      | ヒトツバシ
  ...
[住所から検索] フリーワード絞り込みフィールド
```

### 4. 個別郵便番号ページ（`/zipcode/{7桁}/index.html`）

```
[h1] 〒100-0001の住所 - 東京都千代田区千代田
[パンくず] トップ > 東京都 > 千代田区 > 〒100-0001
[メイン情報]
  郵便番号: 〒100-0001
  住所: 東京都千代田区千代田
  読み: トウキョウト チヨダク チヨダ
  旧郵便番号: 〒100
  全国地方公共団体コード: 13101
[同じ市区町村の他の郵便番号]（内部リンク）
[近くの郵便番号]（前後の番号へのリンク）
[構造化データ] JSON-LD PostalAddress
```

---

## SEO設計

### title テンプレート

```
トップ:      「郵便番号検索 - 全国の郵便番号をかんたん検索」
都道府県:    「{都道府県名}の郵便番号一覧 | 郵便番号検索」
市区町村:    「{都道府県名}{市区町村名}の郵便番号一覧 | 郵便番号検索」
個別番号:    「〒{XXX-XXXX}の住所は{都道府県名}{市区町村名}{町域名} | 郵便番号検索」
```

### meta description テンプレート

```
トップ:      「全国の郵便番号を住所・番号から検索できます。都道府県別の一覧や、住所からの逆引き検索に対応。」
都道府県:    「{都道府県名}の郵便番号一覧です。{市区町村名1}、{市区町村名2}など{N}市区町村の郵便番号を掲載。」
市区町村:    「{都道府県名}{市区町村名}の郵便番号一覧です。{町域名1}（〒{番号1}）、{町域名2}（〒{番号2}）など{N}件を掲載。」
個別番号:    「〒{XXX-XXXX}の住所は{都道府県名}{市区町村名}{町域名}（読み: {カナ}）です。」
```

### 構造化データ（JSON-LD）

個別郵便番号ページに PostalAddress を埋め込む:

```json
{
  "@context": "https://schema.org",
  "@type": "PostalAddress",
  "postalCode": "100-0001",
  "addressRegion": "東京都",
  "addressLocality": "千代田区",
  "streetAddress": "千代田"
}
```

### 内部リンク構造

- 全ページにパンくずリスト
- 都道府県ページから全市区町村へリンク
- 市区町村ページから全郵便番号へリンク
- 個別番号ページから同市区町村の他番号へリンク（上下5件）
- 個別番号ページから前後の郵便番号へリンク

### サイトマップ

- sitemap_index.xml → 都道府県別 sitemap-{prefecture}.xml
- 各都道府県サイトマップに市区町村ページ + 全郵便番号ページ
- 12万ページ超えるのでインデックスで分割必須

---

## 検索機能（クライアントサイドJS）

### 仕組み

静的サイトなのでサーバーサイド検索は不可。
郵便番号データを都道府県別のJSONチャンクに分割し、JSで読み込んで検索する。

### JSONチャンク設計

```
/data/search/01-hokkaido.json
/data/search/02-aomori.json
...
/data/search/47-okinawa.json
```

各JSONの構造:
```json
[
  {"z": "0010000", "a": "北海道札幌市北区", "k": "ホッカイドウサッポロシキタク"},
  {"z": "0010010", "a": "北海道札幌市北区北十条西", "k": "ホッカイドウサッポロシキタクキタジュウジョウニシ"},
  ...
]
```

キー名を短縮してファイルサイズを削減（z=zipcode, a=address, k=kana）。

### 検索ロジック

1. 郵便番号入力（数字のみ）→ 前方一致で絞り込み
2. 住所入力（漢字/カナ）→ 部分一致で絞り込み
3. 入力3文字以上で発火
4. 初回検索時に該当都道府県のJSONをfetch（遅延読み込み）
5. 全国検索の場合は全JSONをfetch（重いのでプログレス表示）

---

## 技術スタック

### ビルド（Python）

```
python 3.10+
pykakasi          # カナ→ローマ字変換（スラッグ生成用）
jinja2            # HTMLテンプレートエンジン
```

### フロントエンド

```
HTML5 + CSS3（フレームワーク不使用、軽量に）
Vanilla JS（検索機能のみ）
```

### デプロイ

```
Cloudflare Pages（GitHub連携）
```

---

## ディレクトリ構成（ソースコード）

```
postal-code-search/
├── CLAUDE.md                    # この設計書
├── data/
│   └── raw/
│       └── utf_ken_all.csv      # 日本郵便CSVデータ（gitignore対象）
├── src/
│   ├── download_data.py         # 日本郵便CSVダウンロード＆解凍
│   ├── parse_csv.py             # CSV解析→構造化dict
│   ├── generate_slugs.py        # 市区町村スラッグ生成
│   ├── generate_html.py         # メインのHTML生成スクリプト
│   ├── generate_search_json.py  # 検索用JSONチャンク生成
│   ├── generate_sitemap.py      # サイトマップ生成
│   └── build.py                 # 全ビルド統合スクリプト
├── templates/
│   ├── base.html                # 共通レイアウト
│   ├── top.html                 # トップページ
│   ├── prefecture.html          # 都道府県ページ
│   ├── city.html                # 市区町村ページ
│   └── zipcode.html             # 個別郵便番号ページ
├── static/
│   ├── css/
│   │   └── style.css            # メインCSS
│   └── js/
│       └── search.js            # 検索機能JS
├── dist/                        # 生成された静的ファイル（デプロイ対象）
└── requirements.txt
```

---

## ビルド手順

```bash
# 1. データダウンロード
python src/download_data.py

# 2. 全ビルド（parse → generate → sitemap）
python src/build.py

# 3. ローカル確認
cd dist && python -m http.server 8000

# 4. デプロイ（Cloudflare Pages）
# GitHubにpush → 自動デプロイ
```

---

## CSV解析の注意点

日本郵便CSVには以下の「悪名高い」問題がある。対処を必ず実装すること:

### 1. 町域名の複数行分割

町域名が38文字を超える場合、複数行に分割される。
カッコが閉じていない行は次の行に続く。

```python
# 対処: レコードを順に読み、未閉じのカッコがあれば次行と結合
```

### 2. 「以下に掲載がない場合」

町域名が「以下に掲載がない場合」のレコードは、その市区町村の代表郵便番号。
町域名を空文字に変換する。

### 3. カッコ内の注記

町域名にカッコ付き注記が含まれる（「大宮町（今出川通河原町西入、...）」等）。
注記はnoteフィールドに退避し、町域名本体のみを表示。

### 4. 一つの郵便番号で複数町域

フラグ12が「1」のレコードは、一つの郵便番号が複数の町域に対応。
個別番号ページに全町域を列挙する。

### 5. 「○○の次に番地がくる場合」等

特殊な町域名表記がある。そのまま表示で問題ない。

---

## 広告・収益設計

### Phase 1（公開直後）

- 忍者AdMax（審査なし）をヘッダー下 + サイドバー + フッター上に配置
- 各ページに広告スロット用のdivを予め配置しておく

### Phase 2（PV育成後）

- Googleアドセンス申請（月1万PV目安）
- 引越し見積もりアフィリエイト（A8.net等）
  - 個別番号ページの下部に「この地域への引越し」バナー
- 転居届・住所変更関連の記事コンテンツ追加

### Amazon アソシエイト

- 郵便番号関連の需要は低いため、このサイトではAmazon導線は控えめ
- 「引越し準備グッズ」程度のまとめリンクをコラムページに配置

---

## デザイン方針

### カラー

```css
--primary: #cc0000;       /* 日本郵便の赤をモチーフ */
--primary-dark: #990000;
--bg: #ffffff;
--bg-alt: #f5f5f5;
--text: #333333;
--border: #dddddd;
--link: #0066cc;
```

### レイアウト

- モバイルファースト
- 最大幅960px、中央寄せ
- テーブルはレスポンシブ（横スクロールなし、スマホでは縦積み）
- ページ速度最優先: 画像なし、CSS最小、JSは検索のみ

### 共通パーツ

- ヘッダー: サイト名 + 検索窓（常時表示）
- パンくずリスト: 全ページに配置
- フッター: サイト情報 + 都道府県リンク一覧 + 免責事項

---

## Phase分け

### Phase 1（MVP・このCLAUDE.mdの範囲）

- [x] CSVダウンロード＆パース
- [x] 都道府県ページ生成（47ページ）
- [x] 市区町村ページ生成（約1,900ページ）
- [x] 個別郵便番号ページ生成（約12万ページ）
- [x] トップページ
- [x] 検索機能（JS）
- [x] サイトマップ
- [x] 基本CSS
- [x] 広告スロット（忍者AdMax）
- [x] Cloudflare Pagesデプロイ

### Phase 2（PV育成後）

- [ ] 事業所郵便番号データの追加
- [ ] 逆引き検索の強化（住所→郵便番号の専用ページ群）
- [ ] 「この地域の情報」コラム自動生成（Claude API）
- [ ] 地図表示（Leaflet.js + OpenStreetMap）
- [ ] Googleアドセンス申請・切り替え
- [ ] 引越しアフィリエイト導線
- [ ] データ自動更新（GitHub Actions + 毎月のCSV取得）

### Phase 3（横展開）

- [ ] 英語版（外国人向け Japan Postal Code Search）
- [ ] 関連サイト #10 銀行コード検索との相互リンク
- [ ] 関連サイト #12 鉄道駅データベースとの相互リンク

---

## 品質チェックリスト

ビルド完了後に以下を確認:

- [ ] 全47都道府県ページが生成されているか
- [ ] 市区町村数が1,700以上あるか
- [ ] 郵便番号 100-0001（千代田区千代田）のページが正しいか
- [ ] 郵便番号 600-8127（京都市下京区）のような複雑な注記が正しく処理されているか
- [ ] 「以下に掲載がない場合」が正しく処理されているか
- [ ] カナ読みが全角カタカナで表示されているか
- [ ] パンくずリストが全ページで正しいか
- [ ] サイトマップが有効なXMLか
- [ ] 検索機能が動作するか（郵便番号検索・住所検索）
- [ ] モバイル表示が崩れていないか
- [ ] Lighthouse Performance 90+ か

---

## ドメイン候補

以下から選定（取得可能なものを確認）:

1. `yubin-bango.jp`
2. `postal-code.jp`
3. `zipcode-search.jp`
4. `yuubinbangou.com`
5. `〒search.com`（非推奨: 特殊文字）

※ 取得はオーナーが手動で行う。

---

## 免責事項テンプレート

```
当サイトの郵便番号データは日本郵便株式会社が公開している
郵便番号データに基づいて作成しています。
データの正確性には万全を期していますが、
最新の情報は日本郵便の公式サイトをご確認ください。
郵便番号データの著作権は日本郵便株式会社に帰属しませんが、
当サイトの独自コンテンツの著作権は当サイト運営者に帰属します。
```
